function handleCRUD() {
    switch (key) {
        case GET_ALL_USERS:
            return allUser

        case DELETE_USER:
                return allUser-single
    
        case ADD_USER:
            return allUsers+user
        default:
            break;
    }
}